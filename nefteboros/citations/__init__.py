"""Anti-hallucination валидатор источников.

Каждая ссылка вида `[Отчёт OPEC MOMR, март 2025]` или `[Источник: Reuters, web]`
в ответе агента проверяется на соответствие реально извлечённым чанкам/web-результатам.

Если источник заявлен, но не подтверждён — ответ переписывается с пометкой
«источник не найден» (или удаляется ложная цитата).

Будет содержать:
  - validator.py — основной валидатор
  - patterns.py  — regex для извлечения цитат из текста
  - rewrite.py   — функция переписывания ответа при провале валидации

Метрика: precision/recall, см. scripts/eval/eval_citations.py
"""
