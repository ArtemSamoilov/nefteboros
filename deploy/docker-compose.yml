# nefteboros — docker-compose
#
# PLACEHOLDER. Реальная реализация — в PR `feature/docker-compose`.
#
# Сервисы:
#   web       — Ouroboros web UI с подключённым skill neftegaz_analyst
#   bot       — Telegram-бот (aiogram), отдельный процесс, шерит RAG-данные с web
#   chroma    — ChromaDB persist (опционально внешний — иначе встроенный в nefteboros)
#
# Volumes:
#   data/corpus/      — PDF (read-only mount)
#   data/vectorstore/ — Chroma persist
#   .embeddings_cache — кеш скачанных embedding-моделей
#
# Запуск:
#   docker compose -f deploy/docker-compose.yml up -d
#   docker compose -f deploy/docker-compose.yml logs -f
#   docker compose -f deploy/docker-compose.yml down

version: "3.9"

services:
  # web:
  #   build:
  #     context: ..
  #     dockerfile: deploy/Dockerfile.app
  #   env_file: ../.env
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ../data:/app/data
  #     - embeddings_cache:/app/.embeddings_cache
  #   restart: unless-stopped

  # bot:
  #   build:
  #     context: ..
  #     dockerfile: deploy/Dockerfile.app
  #   env_file: ../.env
  #   command: python -m nefteboros.bot.main
  #   volumes:
  #     - ../data:/app/data
  #     - embeddings_cache:/app/.embeddings_cache
  #   restart: unless-stopped
  #   depends_on:
  #     - web

  placeholder:
    image: alpine:latest
    command: echo "nefteboros docker-compose placeholder, see feature/docker-compose PR"

volumes:
  embeddings_cache:
