# 2026-05-11 — examples/ (Langfuse-диалоги и ТЗ-сценарии)

## Задача

ТЗ §4.6 формально требует «минимум 5 примеров диалогов» под 5 категорий:

1. Ответ на основе отчёта (RAG).
2. Ответ на основе веб-поиска.
3. Комбинированный ответ.
4. Вызов расчётного модуля с прогнозом цены.
5. Корректная обработка запроса вне компетенции.

До этой задачи раздел `examples/` отсутствовал. Координатор поставил:

- собрать 5+ реальных диалогов из Langfuse за 7 дней;
- прогнать 5 ТЗ-сценариев на актуальном prod (v2.3.5+) и приложить trace_id + dump.

## Что сделано

### 1. `examples/dialogues/` — 9 файлов из Langfuse (7 дней)

Каждый файл — markdown с полным заголовком, meta (trace_id ссылка в Langfuse, session_id, ts UTC, tools), полным текстом ответа агента, RAG/web-источниками из trace и комментарием «что показывает».

| Файл | Категория ТЗ | Trace observations |
|---|---|---|
| `01-rag-opec-momr.md` | §4.6.1 RAG | 12 (trace `b24a9c15` после правки PR #61) |
| `02-combo-rag-web.md` | §4.6.3 Combo | 11 |
| `03-forecast-brent-6m-auto-web.md` | §4.6.4 Forecast | 14 |
| `04-forecast-sarimax-honest-limitation.md` | §4.6.4 Forecast (clean) | 9 |
| `05-multi-tool-sanctions-forecast.md` | Bonus многоинструментный | 12 |
| `06-web-only-current-brent.md` | §4.6.2 Web | 7 |
| `07-refusal-known-observability-gap.md` | §4.6.5 Refusal | — (no trace, известный gap) |
| `08-multi-turn-forecast-context.md` | **Bonus** — multi-turn (3 round, одна сессия) | R1=полный, R2/R3 partial (см. файл) |
| `09-conflict-rag-vs-web.md` | **Bonus** — conflict resolution (CREA vs Argus) | 19 (тот же trace что dialogue 02) |

Все 5 категорий ТЗ покрыты + три **bonus**-сценария: multi-tool, multi-turn, conflict-resolution. Диалог 5 — Bonus многоинструментный (RAG+Web+Forecast в одном запросе). Диалог 7 — честная фиксация регрессии observability для коротких refusal (см. §«Известные ограничения» ниже). Диалог 8 — partial finding: context retention доказан, full empirical multi-turn не воспроизведён из-за WS-layer issue. Диалог 9 — переиспользует trace dialogue 02 с другим фокусом (conflict-handling).

### 2. `examples/scenarios/` — 5 ТЗ-сценариев (свежий прогон)

Прогон выполнен 2026-05-11 13:03–13:11 UTC через WS endpoint prod `ws://186.246.2.190:8000/ws`. Используемая версия — **commit `c3c22f6`** (после `ba783d4` v2.3.5; image `nefteboros:dev` собран 12:48 UTC).

| # | TZ-цитата / canonical | trace_id | tools | duration |
|---|---|---|---|---|
| 1 | §4.6.1 RAG (canonical) | `fd71062e3176f3f3d85e0e9b19158f29` | rag_search + web_search | 171.7s |
| 2 | §4.6.2 Web (canonical) | `9263be9dbd02716e8b11bb0d416e00a3` | web_search | 35.1s |
| 3 | §4.6.3 Combo (canonical) | `2e8486528a4a8270f0dbd79c2c4a41d1` | rag_search + web_search | 195.8s |
| 4 | §2.5 буквальная цитата «спрогнозируй цену Brent на 3 месяца» | `dd8cc6e39dc5dcab0be96c81aff8892b` | analyst_query + classify_intent + forecast_call + validate_citations | 73.3s |
| 5 | §4.6.5 Refusal (canonical «погода в Москве») | — (нет в Langfuse) | — | 12.3s |

В каждом `scenarios/*.md`: цитата ТЗ → запрос → метаданные прогона → полный ответ агента → RAG/web-источники из trace → разбор «прошёл ли ожидания ТЗ».

### 3. `examples/screenshots/` — WS dump'ы (5 файлов)

`scenario-tz-XX.txt`: точный JSON отправленный по WS + точный ответ агента + timing-meta. UI-скриншоты `.png` не приложены — Chrome MCP / preview-инструменты не подключились в окне работы. Воспроизведение тривиально (см. `examples/README.md`).

### 4. `examples/README.md` — индекс

Две таблицы (dialogues / scenarios) с прямыми ссылками в Langfuse, мета-таблица прогона (commit, image build time, server URL), пояснение про канонические запросы (буквальной цитаты ТЗ только одна — §2.5), и one-liner для воспроизведения.

## Ключевые наблюдения из прогона

1. **Auto-enable web в RAG-запросах работает (PR #48).** Сценарий 1 (RAG про OPEC MOMR) автоматически подтянул web_search — корректное поведение для запросов, которые недостаточно покрываются корпусом из 25 PDF (см. dialogue 01 — агент явно говорит «MOMR в корпусе отсутствует» и идёт в web).
2. **Forecast graph hierarchy сохраняется в Langfuse полностью.** Сценарий 4 — 11 observations, видны узлы `classify_intent → forecast_call → validate_citations`. Полностью соответствует §2.5 ТЗ.
3. **SARIMAX отсутствует в deployment**, агент возвращает прогноз через `ou_regime` с явным declaration. Это видно в dialogue 04. Не блокер для ТЗ (требуется минимум два метода — `ou_regime` + `naive` присутствуют), но fix для следующей итерации.
4. **Combo (rag+web) — самый дорогой по времени**: 195.8s на сценарии 3. Связано с двумя последовательными tool-loops, не параллельным выполнением.

## Известные ограничения

- **Refusal trace gap.** Короткие refusal-диалоги (~10–15s, 200–250 chars) теряются в Langfuse из-за async batch-flush. Уже частично закрыто `2026-05-10-wsrunner-eval-observability.md` (flush в handle_task wrapper) + `2026-05-11-observability-post-span-flush.md`, но крайне короткие WS-сессии всё ещё проскальзывают. Функционально refusal работает (см. dialogue 07).
- **UI screenshots .png не приложены** — Chrome MCP не подключился в окне работы. CLI dump'ы по каждому scenario — в `screenshots/*.txt`.

## Новые наблюдения по результатам правок PR #61 (бэклог v2.4)

1. **Presentation-layer leak validate_citations warnings → user-facing answer.** Verified **systematic** через статический анализ 30 traces за 7 дней: фраза «помечены как потенциально «галлюцинированные цитаты» — метаданные pipeline не прошли внешнюю валидацию» присутствует в **10 из 10** forecast-traces (включая dialogues 03/04/05). Root cause: synthesize-узел встраивает `validate_citations.validation_warnings` в финальный markdown. Бэклог v2.4: фильтр перед user-facing output.
2. **Tier-1 web sources на free-tier Brave Search API ограничены ru-language pool'ом** (Forbes.ru/Kommersant максимум, без Reuters/Bloomberg напрямую — но они упоминаются в narrative agent'а). Архитектурно tier-фильтр реализован, эмпирически — cost vs quality trade-off. Бэклог v2.4: fallback на premium tier при низкой авторитетности выборки.
3. **Multi-turn в одной WS connection: context retention работает** (R3 видит R1+R2 в intent classification), но **WS-layer chunk stream хрупкий** для коротких continuation-запросов. Empirical full multi-turn chejn не завершён в окне (4 retake-попытки → broken WS recv). Бэклог v2.4: либо reconnect-per-round, либо чистый `done=true` сигнал на assistant chunk.
4. **Conflict resolution между источниками работает** (CREA vs Argus в dialogue 09 — обе таблицы, разбор методологии, причина расхождения). Жёсткие RAG-vs-Web конфликты не покрыты эмпирически. Бэклог v2.4: adversarial conflict test set.

## Не в scope этого PR (отмечено как backlog)

- PNG screenshots в `examples/screenshots/` — follow-up PR при необходимости.
- Тематическое разнообразие (газ / компании) в dialogues — v2.4.
- Adversarial refusal tests — v2.4.
- Sensitivity / conditional forecast analysis — частично покрыто dialogue 08 R2 (conditional ОПЕК+500), но empirical chejn не завершён.

## Файлы

```
examples/
├── README.md
├── dialogues/  (7 .md)
├── scenarios/  (5 .md)
└── screenshots/ (5 .txt — WS dumps; .png отсутствуют, см. README)
```
