# ADR-0025 — README sync + docs/modules как карта пайплайнов для оценщика

- **Дата:** 2026-05-11
- **Статус:** Принято
- **Контекст:** Финальный pre-deadline (v2.3.5) sync README + создание карты модулей для оценщика Сбера.
- **Связано:** [ADR-0001 (fork)](0001-fork-ouroboros.md), [ADR-0007 (LLM)](0007-llm-providers.md), [ADR-0014 (LangGraph subgraph)](0014-langgraph-subgraph.md), [ADR-0024 (OU regime)](0024-ou-regime-forecast.md), [ADR-0024 (observability)](0024-observability-langfuse.md).

## Контекст и проблема

К v2.3.5 README расходился с кодом по трём пунктам:

1. **Forecast:** README заявлял «SARIMAX + GBR ensemble», но production-путь после ADR-0024 — `regime-conditioned Ornstein-Uhlenbeck`. SARIMAX/GBR остались только для backtest.
2. **Команда индексации:** `python scripts/index_corpus.py` — файла нет; реальный скрипт `scripts/build_index.py`.
3. **Telegram-бот / WIP status:** README показывал `Status: WIP` и общий warning «в разработке», обещал работающий Telegram-бот. На prod-инсталляции Telegram-бот **отключён** из-за RKN-блока исходящих к Bot API. Релиз v2.3.5 — production-ready (по тэгам и pipelin'ам), `WIP` устарел.

Параллельно — ТЗ §3 требует обоснования выбора LLM, и оценщик-аналитик не имеет «карты пайплайнов» (где какие подграфы, какие метрики, что покрыто eval'ом). Это критически важно для оценки production-readiness в дедлайн-окно.

## Решение

### 1. README

- Снять `Status: WIP` badge → `Release v2.3.5`.
- Заменить заявленный forecast-метод на OU с явной ссылкой на ADR-0024 и кратким обоснованием.
- Заменить команду индексации.
- Раздел Telegram — честно: реализован, в prod-инсталляции отключён, причина RKN.
- Добавить раздел **«Выбор LLM»** с тремя провайдерами и обоснованием prod-конфигурации.
- Расширить раздел **«Метрики качества»** — таблица с per-pipeline скриптами, добавить baseline v2.3.5 цифры и placeholder ссылку на `docs/eval-results-v2.3.5.md`.
- Добавить раздел **«Архитектура модулей»** со ссылками на `docs/modules/*.md`.

### 2. Каталог `docs/modules/`

Один файл на пайплайн / подграф / самостоятельный module:

- `analyst_graph.md` — головной LangGraph.
- `routing.md` — classify_intent + llm_disambiguate.
- `rag.md` — retrieval pipeline.
- `web_search.md` — Brave + tiers + lang.
- `forecast.md` — OU production + statmodels backtest.
- `citation.md` — D6 validator.
- `consolidator.md` — Ouroboros background.

**Структура каждого файла (обязательная):**

1. Назначение (1-2 строки).
2. Точка входа (file:line).
3. Входы / выходы.
4. Ключевые ADR (ссылки).
5. Метрики — **обязательно** маркер «есть / частично / нет инструментации», с указанием span name и file:line, плюс eval-скрипт и output-формат.
6. Известные ограничения (если есть).

Правило **«явный сигнал, если метрик нет»** — оценщик сразу видит gap'ы без чтения кода.

### 3. ADR номер 0025

Существуют коллизии: два `0016-*.md` и два `0024-*.md`. Чтобы не множить, **новый ADR берёт следующий свободный номер `0025`** и не переименовывает существующие. Renaming legacy ADR — отдельная задача (risk: ломает ссылки в коде и changelog'ах).

## Почему так

- **README как лицо проекта.** Оценщик из Сбера открывает README первым делом. Лживые claims (SARIMAX, несуществующий скрипт, WIP status на production-ready релизе) подрывают доверие сильнее, чем технические недочёты.
- **docs/modules как «карта для рецензента».** Без неё нужно читать `analyst_graph.py`, `plugin.py`, `nefteboros/observability/` параллельно, чтобы собрать картину. С ней — один файл на пайплайн, один взгляд на «что измеряется, что нет».
- **Маркер «нет инструментации»** — это не недостаток, это явный сигнал. Better honest gap than hidden gap. Consolidator действительно не покрыт eval'ом; pretend в README этого не починит, но запутает рецензента.
- **Структурный invariant** (5 обязательных секций в каждом module-doc) — чтобы при росте `docs/modules/` качество не размывалось.

## Альтернативы рассмотренные

- **Inline в `architecture.md`** (без выделенного `docs/modules/`). Отвергнут: `architecture.md` — это Mermaid-схема + общая картина; per-pipeline детали с метриками туда не помещаются без потери читаемости.
- **Wiki / отдельный сайт документации.** Отвергнут: лишний инфраструктурный шаг до дедлайна; маркдаун в репо проще для оценщика (один git clone).
- **Переименование коллизионных ADR (0016, 0024)** в этом PR. Отвергнут: ломает ссылки в коде (`api.py:5`, `analyst_graph.py:4`, changelog'ах). Отдельная задача с trace всех ссылающихся файлов.

## Слабые места

1. **Placeholder-ссылка** на `docs/eval-results-v2.3.5.md` — этот файл создаёт параллельная сессия. До её мёрджа в README будет битый линк.
2. **ADR-0025 не исправляет коллизии 0016/0024** — оставляет долг.
3. **Маркер «нет инструментации»** в module-docs опирается на статичный grep по `@observe` / `traced_tool` / `log_llm_usage`. Если кто-то добавит другой механизм трейсинга, ярлык устареет — должен обновляться вручную.
4. **Структура `docs/modules/` не сцеплена с CI**. Можно добавить module-doc на новый пайплайн и забыть. На будущее: CI-чек «каждому новому файлу в `nefteboros/graphs/nodes/` соответствует module-doc» — отложен в backlog v2.6.

## Acceptance

- [x] README прошёл фактологический пересмотр (SARIMAX, build_index, Telegram).
- [x] `docs/modules/` создан, 7 файлов соответствуют структурному invariant'у.
- [x] Каждый модуль явно помечен по статусу инструментации (есть/частично/нет).
- [x] Найденные несоответствия (коллизии ADR, placeholder eval_citations, etc) перечислены в changelog для координатора, **не починены в этом PR**.
- [ ] PR ревью координатора (после открытия).
